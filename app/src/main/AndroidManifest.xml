<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="tech.salroid.filmy">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--<uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />-->

    <application
        android:name=".FilmyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:resizeableActivity="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Base"
        android:usesCleartextTraffic="true"
        tools:targetApi="n">

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity
            android:name=".ui.FilmyIntro"
            android:label=" " />

        <activity
            android:name="tech.salroid.filmy.ui.activities.MainActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="tech.salroid.com"
                    android:scheme="http" />
            </intent-filter>
        </activity>

        <activity
            android:name="tech.salroid.filmy.ui.activities.MovieDetailsActivity"
            android:label=" "
            android:theme="@style/DetailsActivityTheme" />

        <activity
            android:name="tech.salroid.filmy.ui.activities.CharacterDetailsActivity"
            android:label="@string/details"
            android:theme="@style/AppTheme.Base" />

        <activity
            android:name="tech.salroid.filmy.ui.activities.FullCastActivity"
            android:label=" "
            android:theme="@style/AppTheme.Base" />

        <activity
            android:name="tech.salroid.filmy.ui.activities.FullCrewActivity"
            android:label=" "
            android:theme="@style/AppTheme.Base" />

        <activity
            android:name="tech.salroid.filmy.ui.activities.FullMovieActivity"
            android:label="@string/title_activity_full_movie"
            android:theme="@style/AppTheme.Base" />

        <activity
            android:name="tech.salroid.filmy.ui.activities.FullScreenImage"
            android:label=" "
            android:theme="@style/FullScreenTheme" />

        <activity
            android:name="tech.salroid.filmy.ui.activities.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:theme="@style/Filmy.Material3.Preference" />

        <activity android:name="tech.salroid.filmy.ui.activities.License" />
        <activity android:name="tech.salroid.filmy.ui.activities.AboutActivity" />
        <activity android:name="tech.salroid.filmy.ui.activities.CollectionsActivity" />
        <activity android:name="tech.salroid.filmy.ui.activities.LoginActivity" />

        <!--  <receiver android:name=".ui.widget.FilmyWidgetProvider">
              <intent-filter>
                  <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
              </intent-filter>

              <meta-data
                  android:name="android.appwidget.provider"
                  android:resource="@xml/filmy_widget_info" />
          </receiver>-->
    </application>

</manifest>